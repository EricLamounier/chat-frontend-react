import './Person.css'

const Person = (props) => {

    return (
        <a className='person nes-btn is-rounded is-primary'
        onClick={props.startChat}
        >
            { /* TODO */}
        </a>
    )
}

export default Person